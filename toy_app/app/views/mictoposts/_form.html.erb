<%= form_with(model: mictopost, local: true) do |form| %>
  <% if mictopost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mictopost.errors.count, "error") %> prohibited this mictopost from being saved:</h2>

      <ul>
      <% mictopost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :mictopost_content %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :mictopost_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
